{
  "name": "agent-404",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "build:script": "tsx script/build.ts",
    "db:migrate": "tsx migrations/run.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.15",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
